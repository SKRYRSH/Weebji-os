name: Push Reminders
# DISABLED — push notifications handled by Supabase pg_cron + OneSignal
# Delete this file or leave disabled; do not re-enable without removing pg_cron jobs first
on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is disabled — pg_cron handles push reminders'
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Push reminders are handled by Supabase pg_cron. This workflow is a no-op."
